package com.llts.test.base;

import org.openqa.selenium.WebDriver;

public class BaseModule extends CustomDriver {
    protected BaseModule(WebDriver driver) {
        super(driver);
    }
}
